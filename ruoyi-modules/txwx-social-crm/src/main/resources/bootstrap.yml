# Tomcat
server:
  port: 9205

# Spring
spring:
  application:
    # 应用名称
    name: txwx-social-crm
  profiles:
    # 环境配置
    active: @spring.profile@
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: ${NACOS_SERVER_ADDR:-YOUR_NACOS_SERVER_ADDR}
        namespace: @nacos.namespace@
      config:
        # 配置中心地址
        server-addr: ${NACOS_SERVER_ADDR:-YOUR_NACOS_SERVER_ADDR}
        namespace: @nacos.namespace@
        # 配置文件格式
        file-extension: yml
        # 共享配置
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
